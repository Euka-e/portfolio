---
interface Props {
  as?: "section" | "div";
  id?: string;
  class?: string;
}

const { as: Tag = "section", id, class: className } = Astro.props;
---

<Tag id={id} class:list={["mx-auto w-full max-w-[1280px] px-6", className]}>
  <slot />
</Tag>
